# Example Assets

Place optional sample assets for the README or local testing here.

These files are examples only.

They should not be required by the runtime module, and they should not be copied into `dist/` unless the package truly needs runtime helper assets.
